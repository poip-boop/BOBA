# Core runtime
streamlit==1.31.0
chromadb==0.4.0
groq==0.4.0
pdfplumber==0.10.3
python-dotenv==1.0.0
regex==2023.12.25

# ML / NLP (pick smaller, more deploy-friendly models)
spacy==3.7.2

# Small sentence-transformer to reduce download size (faster to install)
sentence-transformers==2.3.0
cross-encoder==0.4.3

# Optional heavy dependency: torch — keep or remove depending on build-time limits
# If deployment fails during pip install (torch is large), remove this line and
# switch to an embeddings API (Groq/OpenAI) instead of local SentenceTransformer.
torch==2.4.1

# SpaCy small English model (install at build time)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1.tar.gz
